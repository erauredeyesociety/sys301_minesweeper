# Research — INDEX (EXTERNAL)

Study of things **outside this repo** — LEGO APIs and hardware, host tooling, techniques others have
proven. Consulted BEFORE building. Cite sources; never present an unverified claim as fact.

| File | Summary |
|---|---|
| [spike3-deploy-and-radio.md](./spike3-deploy-and-radio.md) | Research reasoning on deployment routes and on-hub BLE, split out of the API reference. **Both topics are now settled by measurement** — see ADR-0007 and [../findings/ble-protocol-2026-08-27.md](../findings/ble-protocol-2026-08-27.md). Kept for the routes we rejected and why. |
| [spike3-api-reference.md](./spike3-api-reference.md) | **The API reference the Programmer writes mission code against** — opens with the ground truth **measured off our own hub 2026-08-27** (`release 1.24.0`, build `v1.20.0-1742`, no `spike`/`mindstorms` module → SPIKE 3, decisively), then one section per module — `motor` · `motor_pair` · `color_sensor`+`color` · `runloop`/asyncio · `hub.motion_sensor` · `hub.light_matrix` · `hub.button`/`sound`/`light` · `device` · `orientation` — every signature with **units and ranges**, and every unsourced unit marked `[UNVERIFIED]`. Our own `dir()` dumps settle several standing arguments: `motor.CANCELLED` spelling, `motor.info`/`status` exist, **`runloop` has a fourth function `wait` plus `SUCCESS`/`TIMEOUT` constants LEGO never documents**, **`device` HAS `set_mode`/`write_mode`** (so the "unreachable LPF2 modes" claim is overturned), and **no `app` module at all**. Carries a risk-ranked **how to get code onto the hub without the LEGO app** (RAM paste-run → REPL file write → LEGO's COBS slot protocol → `mpremote`, which **soft-resets by default** → `rshell` → the app → DFU), the hard rules around `boot.py` and firmware message ids 10/20, and a plain statement that **our earlier "a hub program cannot drive its own radio" inference is now in doubt** — `dir(bluetooth.BLE)` is the full GAP+GATT surface, the EPERM report is about a *program slot* and our dump was taken at the *REPL*, and what would settle it. Ends with **20 STILL-UNKNOWN items, each phrased as a concrete read-only probe expression**. Where an adversarial verifier refuted a claim, the corrected version is what appears and the correction is named — 21 of them indexed in § 13 |
| [spike-prime-linux-toolchain.md](./spike-prime-linux-toolchain.md) | How to get Python onto a stock-firmware SPIKE Prime hub from native Ubuntu; Hub OS / API generations; why Pybricks is excluded |
| [detection-and-sweep-techniques.md](./detection-and-sweep-techniques.md) | Floor-target detection with the color sensor, robust edge counting, coverage patterns, odometry and heading |
| [color-discrimination.md](./color-discrimination.md) | Telling sticky-note COLORS apart: sensor modes per API generation, calibration, classification, mounting geometry and the speed it bounds |
| [motion-control-and-odometry.md](./motion-control-and-odometry.md) | Making the drive base go where it is told: gyro heading hold and gain tuning per API generation, turn accuracy and settle/verify, what LEGO does *not* specify about the hub gyro, re-squaring against a wall, wheel/track sensitivity and UMBmark, drive-motor choice |
| [detection-odometry-coverage-2026-09-01.md](./detection-odometry-coverage-2026-09-01.md) | **Design brief for the robot AS BUILT (2026-09-01)** — four sections, each mapped onto the existing pure modules and one thin pure helper apiece, never a framework or `main.py`. **(A) Colour fusion:** make channel **clipping** a specularity partition layered *additively* on `classify.py`'s chromaticity nearest-centroid — `clip ⇒ not-the-matte-mine` is a **one-way** inference (gloss is angle-dependent), clip counted over **R,G,B only** (not intensity `[3]`), silver-vs-blue demoted from a hard key to a soft vote, `reflection()` a calibration-time cross-check not a per-tick read; resolves blue-tape-vs-yellow-note by specularity→hue→width→position and escalates only a matte-blue-decoy case. **(B) Odometry with the scrubbing caster:** the gyro-vs-encoder gap grows on turns **by design**, so position=encoders / heading=gyro-witness, D_eff from a straight, track from a gyro-vs-encoder **regression** (a bending line = the pivot moves), Borenstein Type A (wheelbase, same-sign) / Type B (diameter, reverses) as the fault diagnostic, turns **gyro-closed** so `TURN_ENC_SCALE` only feeds the G1 false-flag guard, two effective tracks kept separate. **(C) Coverage under drift:** `P(S)=S+(W−2e−m)` — two sensors buy **lanes, not looser control**; `r_max=2ev/L²` swings ~170× with the units of "10×10"; one global yaw frame + a per-lane ABSOLUTE fix; two-sensor tape line-squaring is the **only candidate** absolute reference in a wall-less arena but its ~1.9° quantization accuracy is UNMEASURED and inadequate at metre scale. **(D) Boundary detection:** a three-layer (saturation / chromaticity / width) discriminator, a rising-edge `BoundaryWatch` that stops **before** the wheels cross via creep-on-approach, first-sensor-to-trip fail-safe, B1 odometry backstop; the ~9° datum is a startup-ramp shortfall, **not** a coast, so no coast datum exists yet. Everything in encoder-deg/revs where mm is not convertible; confidence split into **structural** (sound) vs **measured** (zero on mission surfaces at the built geometry). Ends with consolidated new signatures, RECOMMENDED CHANGES to other files (not edited — collision-safe), and the single highest-leverage bench measurement: the **GATE-1 real-surface rgbi+reflection burst at ~16 mm** |
| [odometry-fusion-and-health-2026-09-01.md](./odometry-fusion-and-health-2026-09-01.md) | **Design brief for the robot AS BUILT (2026-09-01)** — distance/variable-rate-turn kinematics **and** IMU-vs-encoder fault detection with self-tuning, all as small pure functions on the existing modules (no framework, no `main.py`). Opens with the one code-fact everything rests on: `hub_motors.drive()` applies **no** mirror sign today (it is only a comment), reads are raw, and `Odometry.update()` fed the real mirrored encoders integrates a forward move to **≈0 mm** — a latent bug the fix repairs by applying `LEFT_SIGN=−1`/`RIGHT_SIGN=+1` at the difference step. **The load-bearing correction:** the yaw differential is the SIGN-CORRECTED `right_fwd − left_fwd` (raw-port `raw_R + raw_L`, the SUM), **never** raw `R − L` (which measures forward translation — backwards), or `turn_slip`/`estimate_k_yaw`/`heading_from_encoders` false-fire on every straight and go blind on every spin. **(1) Kinematics:** distance = mirror-per-wheel then mean; turns are **gyro-closed** (the one inversion of encoders-primary, because the scrubbing caster makes an encoder-geometry turn under-rotate by an UNVERIFIED amount); a variable ease-in/cruise/ease-out `turn_speed_profile` shaped on the gyro-angle fraction (S-curve or trapezoid, creep floor) wrapped in a settle-and-verify executor; mirror mount ⇒ both motors take the SAME spin sign; no coast datum exists (the ~9° is a startup ramp, not a stopping coast). **(2) Fault detection:** `turn_slip()` (wheels differentially spinning, body under-yawing) and `disturbance()` (wheels still, IMU moving ⇒ lifted/pushed/tipped), gated by encoder regime, with the false-trigger guards — sign-inversion, the accelerometer being blind to constant velocity (so straight-line slip leans on motor STALL status, not the IMU), end-of-move chassis rock needing a post-settle debounce, G1 gated to straights, and the gyro deadband making a slow turn look stuck. **(3) Self-tuning:** three constant groups (N noise / S scale / D derived), `K_YAW` measured directly as a diameter-free ratio that bakes the caster in, N-sigma-from-MAD with a mandatory deadband floor, thresholds provably independent of wheel diameter; set by a stationary hold + one straight + one logged spin. Ends with the signature table and RECOMMENDED CHANGES (not applied — collision-safe). Everything in encoder-deg/dps/raw-IMU units; challenger corrections folded in |
| [speed-envelope.md](./speed-envelope.md) | How fast the hardware *can* go vs how fast it *should*: official 45602/45603/45607 speed-torque specs, the three candidate wheels and how to tell ours apart, the motor x wheel kinematic ceiling table, derating for mass and rolling resistance, why gearing up is a trap, SPIKE 2 vs SPIKE 3 velocity ceilings — hardware ceiling ~513 mm/s no-load / ~506 mm/s on carpet for an `[ASSUMED]` large motor on Ø56 wheels, and ~300 mm/s as the sweep *target* the trade study needs rather than a measured limit |
| [spin-scan-localization.md](./spin-scan-localization.md) | **Can we make a scanning rangefinder by spinning the robot?** The operator's spin-SLAM proposal, assessed on the evidence: the *mechanism* works and is free (the robot already spins; the 250 mm sensor lead rotates with the hub, so nothing winds), but the 45604 is **ultrasonic** with a **±35° entrance angle** — a ~70° cone giving ~5 independent bearing cells per revolution, no point cloud, and a hard 2000 mm ceiling. Corrects the accelerometer/gyro confusion (yaw comes from the gyro; the accelerometer's job is proving the robot is flat, and gyro-vs-encoder *disagreement* is a free slip meter). Localizing in a known rectangle needs no SLAM — 5 scalars, no matrices, fits the hub easily. **Keeps** forward range reads during a lane and **one** run-start scan — as an exteroceptive *cross-check* on the arena size and a pose fix, not a way of discovering an unknown arena, because the ±476 mm centre window it needs cannot be reached without already knowing the size (SS-7); **rejects** per-lane spin re-squaring (~±3.5° vs the wall bump's 0.6°, for 1.9–3.2 min of run time). Says what [hub-compute-limits.md](./hub-compute-limits.md) § 4.5 must re-word — "ToF" is wrong, "cannot scan" is answerable, "the scan has no features in it" is the argument that holds. **Adversarially audited 2026-08-26**; every LEGO figure re-fetched, 15 corrections applied, verdict unchanged |
| [papers/INDEX.md](./papers/INDEX.md) | **The academic paper corpus** — 12 openly-available PDFs (46.8 MiB, gitignored) with tracked `.txt` sidecars (819 KiB) so the corpus is greppable: coverage path planning and turn costs, UMBmark odometry calibration, MEMS-IMU drift, sensor-footprint coverage. One row per paper saying what we use it for, **what it does not support**, and which of our documents cites it. Source URLs and arXiv IDs re-verified against the PDFs 2026-08-26 |
| [hub-compute-limits.md](./hub-compute-limits.md) | What the hub actually is (STM32F413, 100 MHz Cortex-M4F, 320 KB SRAM, ~250 KiB MicroPython heap) and what fits in it: available modules and the absences that matter (no `numpy`, no threads, slow boxed floats), the loop-rate unknown that `SAMPLE_RATE_HZ` currently papers over, **SLAM rejected** on the sensor suite (no loop closure) — with the compute and memory arguments explicitly *not* load-bearing, and the numbers to show why, the five feasible localization rungs with RAM and loop cost for each, and the program-size ceiling |
| [sensor-mounting-geometry.md](./sensor-mounting-geometry.md) | **Where each sensor physically goes, and what that costs in parts** — the 45605/45604/45606 envelopes, mounting points and captive-cable geometry measured off the official LDraw models (colour = 24.0×23.2×23.6 mm with its pin axis 19.6 mm behind the lens; distance = 56 mm wide; force plunger carries a cross-axle hole), the flat 7.5×1.7 mm ribbon whose fold plane fixes each sensor's roll, role-by-role orientation and standoff with what drifts and why, the finding that **LEGO geometry cannot produce a 5–10° tilt** so the distance sensor must be raised rather than angled, the two-side-sensor heading claim and the repeatability it is gated on, an ambient shroud that provably cannot clip the 12 mm spot, the parts to ask the Supplier to price, the geometric conflicts, and the **orientation summary table** that lets sensor count and orientation be budgeted together. **Adversarially audited 2026-08-26** — every LEGO dimension re-derived from the techspec PDFs, the Element Overview and the LDraw models and **reproduced exactly**; 28 corrections applied, the largest being an inverted floor-clearance rule for the forward distance sensor, `atan`→`asin` in the two-sensor heading geometry, a per-unit sensor bias that does *not* cancel in the difference, and an inverted skid-vs-shroud inequality |
| [ble-bring-up.md](./ble-bring-up.md) | **Why the hub shows no light on its Bluetooth button and advertises nothing — and what to do about it.** Opens with the DFU hazard: **holding the CONNECT button while USB is plugged in is the documented bootloader gesture**, and its pink-green-blue-off cycle shares all three colours with LEGO's "Hub OS updated, restart me" pattern. Then the ranked answer: the leading explanation is that **LEGO's Hub OS was not running**, because every probe in this repo fires `Ctrl-C` twice before it speaks — `[INFERRED]`, resting on one confirmed plank (the hub exposes **one** CDC-ACM function, `0694:0009` "SPIKE Prime VCP", and LEGO's COBS protocol owns that same port, so a `>>>` on it means the Hub OS is not driving it) plus two extrapolations a verifier struck down. Eight hypotheses, each with the test that discriminates it, labelled **read-only / low / medium / high**, DO-NOT-RUN list at the **top**, and the decisive first test costing nothing: **look at the hub** — LEGO documents the centre button as solid green for any healthy powered hub and documents **no dark state**. Identification: LEGO documents exactly one advertising fact (the FD02 service UUID is in the payload), our `device_uuid` is the **STM32 die ID** and physically cannot be advertised, so matching is filter-FD02 → connect → `DeviceUuidRequest 0x1A` → compare 16 bytes; **the BLE address type is UNKNOWN and every "it's a stable MAC" source turned out to be Bluetooth Classic / Hub OS 2 tooling** — struck, though a rotating address costs us only the fast path, not correctness. Concrete `bleak` connect-and-upload with the framing bytes **computed from LEGO's own `cobs.py` on this host** (`InfoRequest` = `00 00 02`, and the earlier hand-typed `02 03 02` was wrong **and contained a raw `0x03` = Ctrl-C**), the chunk-size-must-be-a-multiple-of-4 CRC proof, the reassembly LEGO's own example admits it lacks, and a required **allowlist** so no tool can ever emit firmware message ids 10/20. **Our earlier "a hub program cannot drive its own radio" inference is dead** — `dir(bluetooth.BLE)` is the full GAP+GATT surface — but the verdict is unchanged and now argued on mechanism: a single un-chainable `irq` slot, `gatts_register_services` that resets the GATT DB, a shared singleton, and zero latency or throughput gain over `hub.config['module_tunnel']`, which is already binary and bidirectional. **Don't pursue it before 10 SEP.** § 7 is a corrections register naming every claim a verifier overturned |
| [program-upload-protocol.md](./program-upload-protocol.md) | **How to store a `.py` in a Hub OS program SLOT and RUN it** — the capability the REPL/module route (ADR-0007) lacks, because `/flash/main.py` does not autorun (KU-M16). The full sequence, from LEGO/spike-prime-docs: `DeviceUuidRequest` (identity gate) → `InfoRequest` → `ClearSlotRequest` → `StartFileUploadRequest`(whole-file CRC) → `TransferChunkRequest`×N(running CRC) → `ProgramFlowRequest(Start)`, then `ConsoleNotification` carries `print()` output back. Every message ID/field/width/endianness, the checksum (standard reflected CRC-32 = `binascii.crc32`, seed 0, LEGO's mandatory 4-byte zero-padding), a mermaid sequence diagram, the BLE MTU/throughput analysis (the recorded "MTU 23" is likely a bleak/BlueZ default, not the wire — re-measure; realistic upload gain ~9–13×, telemetry ~2–5×, not the raw-byte 20×), and a 13-item **[UNVERIFIED]** register each with the probe that settles it. Firmware ids `0x0A/0x14` sit next to file-upload `0x0C` and are refused in code. Client: [`../../hub_programmer/slot_upload.py`](../../hub_programmer/slot_upload.py) (dry-run default, USB-primary, `--ble` for untethered, proves identity before writing). **Primary-source + host-checked; NOT run on our hub** — ADR-0007 stays the proven route. Verifier corrections folded in (§ 7) |
| [bluetooth-control-plane.md](./bluetooth-control-plane.md) | **Can we upload and manage code over Bluetooth instead of USB, and stream telemetry off an untethered robot?** LEGO's own BLE protocol reference read at source: GATT service `0000FD02-…` with hub-RX/hub-TX characteristics, write-without-response + notifications, the COBS-with-escapes/XOR-0x03/delimiter framing and standard zlib CRC32, and the complete message id table. **Upload to a slot and start/stop it do work over BLE** — but the real find is **`DeviceNotification` (id 60)**, which streams battery, IMU yaw/pitch/roll + raw accel + raw gyro, every motor's position/speed/power, the colour sensor's **raw R/G/B 0–1023** and distance in mm to the laptop at a requested interval **with no hub-side code at all** — the untethered telemetry the parked [telemetry plan](../plans/telemetry-and-analysis.md) wanted, on a robot with no cable dragging its heading. **Hub OS 3 only** (Hub OS 2 is Bluetooth Classic RFCOMM + JSON-RPC — a different stack), so it is gated on KU-M1, and a BLE scan is itself the generation test. Throughput is governed by LEGO's **stop-and-wait** chunk loop, not by MTU or framing: chunks × RTT, and the RTT is **UNMEASURED** — this host's kernel *prefers* a 30–50 ms connection interval, but no link has ever existed and the hub negotiates its own. No slot listing, no file download, no REPL over BLE — those stay USB. No released version of the VS Code extension implements either telemetry message (an **open, unmerged PR #80** would), and its BLE console readback is broken by another **open, unmerged PR #81**. **Verdict: supplement, don't replace** — dev loop stays USB, add one read-only `bleak` telemetry script, and never let a run depend on the link. Carries a four-phase go/no-go bench procedure that closes KU-M1 without a cable and never risks a Hub OS update prompt. **Nothing tested — no hardware exists** |
| [telemetry-while-driving.md](./telemetry-while-driving.md) | **How to use Bluetooth WHILE the motors run — the operator's direct question — and the telemetry architecture it implies.** Synthesises four adversarially-verified research passes (concurrency, architecture, MTU, literature; all `refuted:false`) against measured ground truth. **Concurrency verdict:** YES, one stock-SPIKE-3 **SLOT** program can drive motors and emit telemetry over BLE at once — because a slot program runs *under* the live Hub OS that owns the radio, whereas the REPL route needs a `Ctrl-C` that kills the Hub OS — and telemetry leaves as ordinary `print()` → `ConsoleNotification`, not user BLE code (which **must not** touch the shared radio singleton). The mechanism is primary-sourced (cooperative single-threaded `runloop`; `motor.run()` fire-and-forget) but the end-to-end path is **INFERRED, never run on our hub**, gated on G4/G4b/G5. **Recommendation:** Design B (log the full record to a **`/flash` file**, retrieve over **USB after the run** — zero cable drag) as the record of account, with a ~3 Hz `ConsoleNotification` heartbeat default **OFF**; full-rate live is infeasible at MTU 23 (~5–7 rec/s). **MTU:** the recorded 23 is a **bleak/BlueZ reporting default, not the wire MTU** (read it with `_acquire_mtu()`/`max_write_without_response_size`); MTU is **not** the throughput lever — connection interval, packets-per-event and DLE are, and 23→247/509 buys only ~20–33% without DLE. Corrects competition-program-design §4.6's "MEASURED MTU 23" over-claim, the ~5–7-vs-~16.7 rec/s computed disagreement, and a mis-cited Nordic/`stdout`-blocking source. Literature (PixHawk/AUV black-box, teleop-latency-is-non-binding, offline lower-envelope clock reconciliation) endorses the log-primary hybrid. Ends with an **18-item [UNVERIFIED] register**, each with its bench test. **Nothing run on hardware** |
| [telemetry-offload-paths.md](./telemetry-offload-paths.md) | **The EXHAUSTIVE set of ways to move data off a driving, untethered hub** — every channel out of the STM32F413 (BLE radio, USB, `/flash`, 5×5 matrix, speaker), ranked Tier 1–5 on stock-firmware feasibility × throughput × simplicity, each with its catch. Recommended pairing unchanged (on-hub `/flash` log → USB after stop, + OFF-by-default `print()` heartbeat); the two live upgrades that need **no hub radio code** are `DeviceNotification` (off-VM witness) and `TunnelMessage 0x32` (binary, bidirectional, `[UNVERIFIED]` our build + crash bug); light-matrix/speaker are **status, not data**; user-code-owns-the-radio and Pybricks `broadcast`/`observe` are ruled OUT (singleton displacement / firmware reflash). **No file-download message exists** — a BLE log dump is a slow `print()` burst, so USB is the only ungated bulk channel. **Nothing run on hardware** |
| [device-notification-telemetry.md](./device-notification-telemetry.md) | **How the SPIKE App live-monitors sensors/motors while a program runs — and whether we subscribe instead of `print()`.** The Hub OS pushes `DeviceNotification` (id 60 / 0x3C): one binary snapshot per requested interval (`DeviceNotificationRequest` id 40 / 0x28, `uint16` ms, 0=off) of battery, 6-axis IMU, every motor's cumulative+abs position/speed/power, colour class + raw RGB 0–1023, distance — **with NO hub-side code, program-independent**. Exact nested byte layouts from `messages.py`. Verdict: a superior OFF-VM path for the hardware channels (removes the G4/G4b `print()`-stall unknowns, the safest first BLE test) but **NOT a `print()` replacement** — it cannot carry program-logic state or a hub timestamp, so **three complementary channels**, not a swap. `bluetooth-control-plane`'s "Hub OS 3 only / generation unknown" caveat is now **CLOSED** (measured SPIKE 3). Same connection-interval floor as any notification — **no throughput win**. Cross-check IMU units before merging; parse defensively (battery-record `struct.error`, cause `[INFERRED]`). 7-item `[UNVERIFIED]` register. **Nothing run on hardware** |
| [compact-telemetry-encoding.md](./compact-telemetry-encoding.md) | **Shrink the telemetry PAYLOAD instead of raising the link.** The sanctioned live channel is TEXT (`ConsoleNotification` = NUL-terminated UTF-8), so a compact record must be re-textualised as **base64** (codec proven on-hub, ADR-0007). A live-essential subset (`seq, state, det_state, lane, count, yaw_ddeg` + flags) packs to **~9 bytes → 12 base64 chars → 16 B framed → ONE notification** vs the full CSV's five — a **~5× live-rate gain** (~6.7 → ~33 rec/s) **with no radio/BlueZ/firmware change**, the single lever fully ours. Fixed-width binary beats a decimal subset by a full notification (deterministic 1 vs value-dependent 2). MTU/DLE are **moot once the record fits one notification**; connection-interval 30→7.5 ms is up to ~4× but `[UNVERIFIED]` (BlueZ-central may ignore it). **Does NOT answer concurrency** — still gated on G4/G4b; build `telemetry_bin.py` the day G4 passes. `framed = text_chars + 4` is HOST-CONFIRMED via `probes/_cobs.py`. **Nothing run on hardware** |
| [slot-execution-and-live-motor-control-2026-09-03.md](./slot-execution-and-live-motor-control-2026-09-03.md) | **Why an uploaded SLOT program Acks every step but never runs — and whether the host can drive a motor with no program at all.** Diagnoses `slot_upload.py`'s failure (motor still, no `ConsoleNotification`, though the *same* program spins over the REPL) against LEGO's own reference `app.py` and PeterStaev's Hub OS 3 extension: **the file is uploaded under the wrong name.** The slot's runnable entry point is the fixed name **`program.py`** (raw source — **`.mpy` is NOT required**, only an optional speed win) or `program.mpy`; our client sends the *source basename*, so `ProgramFlow(Start, slot)` — which selects by **slot number**, not filename — *Acknowledges* but finds no `program.py` to execute. Everything else (message ids, CRC, chunking, the `1E 00 <slot>` ProgramFlow layout with `stop=0`=Start) is already correct. **The one-line fix as a spec** (client not edited): default the upload name to `program.py`. On the alternative — **there is NO host→hub actuation message in LEGO's published protocol**: every `DeviceMotor`/sensor "device message" is read-only telemetry nested in `DeviceNotification (0x3C)`, and `TunnelMessage (0x32)` only pipes bytes to a *running* program, so driving a motor **always** needs a program running — fixing slot execution is the only stock-firmware path. Recommends composing the fix with a zero-hub-code `DeviceNotificationRequest (0x28)` subscription for telemetry-while-driving (exactly LEGO `app.py`'s composition). Primary-sourced at each claim; **NOT re-run on our hub** — 7-item `[UNVERIFIED]` register with the USB-first bench steps |
| [hub-menu-and-buttons-2026-09-03.md](./hub-menu-and-buttons-2026-09-03.md) | **The Hub OS 3 on-brick menu and the untethered START gesture** — how the operator launches a stored slot program from the hub itself, buttons + 5×5 matrix only, no computer. Menu states (**HEART** = home/idle, **digit(s)** = selected slot 0–19, **spinning** = running; center-LED colours green/orange/red/white/**blue**/**purple**, the last two the firmware-update danger zone) with a mermaid `stateDiagram-v2`; the button-gesture table (**LEFT/RIGHT** scroll slots, **single CENTER press** = run selected slot / mid-run = STOP, **CONNECT** = advertise only) and the end-to-end START gesture. **The KEY UNKNOWN, marked [UNVERIFIED]:** can the operator launch *our* protocol-uploaded `program.py` from the buttons, or does menu-launch need the LEGO app's project format? Strong-**INFERRED YES** — the SPIKE app downloads via the *identical* `StartFileUpload "program.py"` sequence, so a fixed-name upload is byte-indistinguishable and a CENTER press is the firmware's local `ProgramFlow(Start, N)`; our hub already proved a slot `program.py` runs and persists 45 s after unplug, leaving only the *button* trigger unproven — settled by **one bench test** (upload to slot 3, unplug, navigate, press CENTER, watch the motor). Plus the power-off hazard (CENTER is the power button: **single crisp press only, never a hold**) and its blacklist cross-check (long-CENTER = lost run; hold-CONNECT-during-USB-plug = firmware danger; any solid-blue/three-colour-cycle = STOP). **Nothing run on hardware** |
| [ble-while-driving-workarounds-2026-09-01.md](./ble-while-driving-workarounds-2026-09-01.md) | **Capstone over the three passes above — the EXHAUSTIVE BLE-while-driving workaround set, ranked, with the bottom line.** §1 ranks every path off a driving hub (table: path / works-on-stock / rough throughput / catch), §2 the LEGO-app `DeviceNotification` mechanism (**usable — subscribe with id 0x28; off-VM hardware witness that sidesteps G4b; but three channels, not a `print()` swap**), §3 the payload shrink (**makes a modest 10–33 Hz live stream viable ~5× for free, but changes how much fits, not whether anything fits — still gated on G4**). **BOTTOM LINE: the log-and-retrieve recommendation stands, unchanged** — the workarounds upgrade the LIVE half, never the record of account; every one is a config value, not a redesign. **The one experiment that decides everything is G4** (spin a motor, `print()` numbered lines, watch for `ConsoleNotification`s), with the zero-hub-code `DeviceNotificationRequest(1000)` companion runnable the same session. Reflects verifier corrections (`TunnelMessage` = 0x32 not 0x50; battery-byte cause `[INFERRED]`; framed-size HOST-CONFIRMED). **Nothing run on hardware** |

| [floor-relative-colour-anomaly-2026-09-03.md](./floor-relative-colour-anomaly-2026-09-03.md) | **Colour-AGNOSTIC mine detection: learn the floor at run start, flag what deviates — for the two day-of unknowns (mine colour hedged twice; venue floor is multicoloured carpet bands) that break any hard-coded threshold.** Fills the one gap in the existing pipeline: `calibration.py` needs a KNOWN target, but on the day we have only the floor. **Recommended metric:** nearest-exemplar **sigma-normalised chromaticity distance** — `deviation(rgbi) = min over floor bands of hypot(r_n−cx, g_n−cy)/sigma` — one scalar per reading, `classify.py`'s own sigma-distance turned inside out (nearest-to-ALL-bands instead of nearest-class), multi-modal by the `min`, brightness/battery/height-invariant because chromaticity divides scale out. **Floor model:** greedy radius-cluster N run-start floor samples into ≤ K_MAX exemplars (one per band), each a median centroid + `1.4826·MAD` spread (`classify.ColorClass` math verbatim); `merge_radius` derived from the floor's own scatter. **Threshold:** self-calibrating `on = median(d_floor) + 8.90·MAD(d_floor)` — `calibration.py`'s 6-SD rule applied to the deviation scalar's own floor distribution; hangs the hysteresis band below it. Feeds `detector.py` **UNCHANGED** via an identity-`signal()` shim (deviation IS the scalar, polarity +1). **Key caveats:** no target ⇒ NO calibrate-time contrast gate (the contrast check moves to run time + the bench test); band seams handled by the EXISTING width gate, not by raising the threshold; the irreducible blind spot is a note whose chromaticity matches a floor band (a professor question, not a knob). 7-item `[UNVERIFIED]` register + the ONE bench test (log floor + a note on each band, compute the metric offline, check ≥6-SD separation). **Nothing measured** — hub has never sampled the floor |
| [compound-mine-signature-2026-09-03.md](./compound-mine-signature-2026-09-03.md) | **Detecting a COMPOUND/SEQUENTIAL colour signature (e.g. yellow-then-blue in one pass) with the one downward sensor, minimally, on top of the existing edge-events + classify pipeline.** For the day-of case where a mine is a yellow note WITH a blue sticker and a plain yellow note is NOT — colours unknown and changeable, mines added/removed mid-run so **no fixed count**. **The model:** a mine is a target ordered colour list matched as a **contiguous subsequence** of a "mine window"; segments come from `classify.py` run on **ordered windows** of one event's buffer (a Y+B mine is ONE floor-anomaly envelope, so the whole-event median blends to UNKNOWN — the ordered head/tail is the signal), with optional cross-event stitching (`SIG_ADJ_MM`, same lane) for the case where exposed floor makes the deviation dip into two events. **The knob:** a single `MINE_SIGNATURE` string in `config.py`, parsed to a `(mode, colours)` tuple — `"any-anomaly"` (default, plain presence, needs NO colour classes so it survives when colour calibration can't be done), `"colour:X"`, `"sequence:X,Y[,Z]"` (length-generic); operator switches rule at 09:00 with no code change, colours are labels for whatever is calibrated on the day. **Dynamic arena:** report-as-you-go — force a pose-tagged `MINE` row through the EXISTING `event_filter` significant-event log the instant `is_mine()` fires; dedupe re-sightings against a flat landmark list by position within `DEDUPE_MM` (**odometry-limited, not sensor-limited** — set from revisit drift); a miss NEVER deletes a landmark (log a negative observation, humans reconcile the append-only log). **What NOT to build:** no image processing / colour-region-adjacency graphs (the one ResearchHub hit, IEE 1999, is exactly that — rejected), no ML, no fixed count, no SLAM / data-association fusion, no second detector, no auto-removal. 6-item `[UNVERIFIED]` register + ONE replay bench test. **Nothing measured** |
Operator's raw platform notes: [../archives/operator-notes/2026-08-25_spike-platform-notes.md](../archives/operator-notes/2026-08-25_spike-platform-notes.md), [../archives/operator-notes/2026-08-25_available-sensors.md](../archives/operator-notes/2026-08-25_available-sensors.md) — superseded by the files above.

**Watch the API generation.** Most SPIKE tutorials online target the legacy SPIKE 2 API
(`from spike import PrimeHub`). The current API is `import motor` / `from hub import port` /
`import runloop`. Check which one a source targets before believing it.

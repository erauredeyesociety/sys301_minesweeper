# Runbooks — INDEX

Repeatable operator procedures. Written so a teammate can follow them under time pressure without
context. Every hub-touching step **an agent runs** goes through a script with an explicit timeout
([../directives/automation-first.md](../directives/automation-first.md)); steps a human performs at the
machine state the observation to expect instead.

| Runbook | When to run it | Owner | Status |
|---|---|---|---|
| [identify-hardware-after-rebuild.md](./identify-hardware-after-rebuild.md) | **Start of any session where the robot may have been disassembled/reassembled** — a rebuild lands motors and colour sensors on different ports. Orchestrates the read-only probes (`probes/devices.py`) to discover the port map, says which constants in `docs/hardware/port-map.md` and `src/hub_api.py` to update, and covers the wheels-up drive check that `device.id()` **cannot** substitute for (both motors read id 48, so LEFT/RIGHT + forward-sign must be re-watched). | **Programmer** (probes, edits) · **Builder** (drive check) | Written 2026-09-03 · cites existing probes and `examples/drive_moves.py`; steps **UNRUN this session** (no hub touched writing it) |
| [hub-identification.md](./hub-identification.md) | First time the hub is connected — **READ-ONLY**, writes nothing, must not trigger a Hub OS update. Resolves the `[UNKNOWN]` SPIKE 2 vs SPIKE 3 API generation that blocks all mission code. | Programmer (plugs in), operator | **EXECUTED 2026-08-27** — result: [../findings/hub-first-contact-2026-08-27.md](../findings/hub-first-contact-2026-08-27.md). Our hub is **SPIKE 3 / MicroPython 1.24.0**. Revised the same day: the REPL and 115200 8N1 markers are struck, and **three of its predictions were WRONG** (`hub.info()`, both battery call forms, and "the identify scripts don't exist") — each marked in place |
| [deploy-to-hub.md](./deploy-to-hub.md) | **Every time you put code on the hub.** The proven USB route with **no LEGO app**: baseline capture → `hub_programmer/upload.py` (module → `/flash/lib`, verified by SHA-256 computed *on the hub*) → `probes/import_check.py` → `hub_programmer/run.py` (program in RAM via paste mode, leaves nothing behind). Covers the refusals, a troubleshooting table, and the re-capture + diff that proves the firmware untouched. | **Programmer** (may plug/unplug) | **PROVEN 2026-08-27** on our hub over `/dev/spike` — `src/config.py`, 13262 B / 70 chunks / 3.6 s, on-hub SHA-256 matched, `OK config`. **Standalone boot from `/flash/main.py` is [UNVERIFIED]** — the deploy story is only half closed |
| [upload-to-hub.md](./upload-to-hub.md) | Getting code onto the hub — Linux **and** Windows. No pip, no packages: one file per slot, 20 slots, runs untethered | Programmer | **SUPERSEDED in practice by [deploy-to-hub.md](./deploy-to-hub.md)** · written 2026-08-25 with no hub; its 20-slot model is contradicted by measurement (`/flash/program` is empty) and its "the hub has never been connected" header is stale |
| [demo-day.md](./demo-day.md) | End of every class (pack-out check), start of class 10 SEP, and before/after every dry run | **Builder** — the only authorised robot operator | Drafted · mission- and arena-dependent steps **PENDING** the instructor's briefing |
| [measure-drivetrain.md](./measure-drivetrain.md) | The first class session at which a driving chassis and the hub exist together — **planned 3 SEP**. **No colour sensor is needed for any step in it** (only the plan's BM-5 sensing-loaded loop rate and BM-6 spot size want one), so it must not be held behind that purchase. Measures the drivetrain constants the whole sweep design rests on: effective rolling diameter under load, track width from a spin turn that actually closes, top ground speed (saturation vs control loss), and cross-track error over a real lane. | **Builder** operates; **Programmer** at the laptop | Written 2026-08-25 · **UNRUN, no hub, no robot** — every hub-side step is **UNVERIFIED**. Plan and drop order: [../plans/bench-measurement-plan.md](../plans/bench-measurement-plan.md) |
| [researchhub-tunnel.md](./researchhub-tunnel.md) | Only when the literature workflow is actually needed — a real literature review for the Intro Report, or a search `grep` over `docs/` could not answer. **Not needed to build the robot.** Covers the SSH forward to ResearchHub on pwnstar (up / status / stale-repair / port discovery) and `fetch_paper.py`. | Programmer | Verified 2026-08-25, **independently re-verified** the same day (discover/up/status/restart/down, `/health`, a real search, and the fetcher all re-run) · port **5347** confirmed by probe · link is **flaky**, expect `restart` |
| [docs-rag.md](./docs-rag.md) | Any time you would otherwise `cat` or grep a long file in `docs/` — ask the project's own docs a question and get back a file path + line span. Covers start / stop / re-ingest / query / when it breaks. | Whoever is searching (no hub, no roles) | **Search VERIFIED 2026-08-25**, independently re-verified same day (down/up round-trip, real queries, hand-checked citations, read-only mount proven) · `/api/ask` **returns HTTP 500** — no LLM model pulled · ollama dies on reboot, restart it with `./ollama-serve.sh` · ollama costs **~2 GB**, which `rag doctor` does not count |

The deploy route was chosen by demonstration, not by reading: REPL + base64 chunks → `/flash/lib` →
on-hub SHA-256 verify → import check, first run 2026-08-27. It is written up in
[deploy-to-hub.md](./deploy-to-hub.md); the background study that preceded it is
[../research/spike-prime-linux-toolchain.md](../research/spike-prime-linux-toolchain.md).
- [first-main-run.md](./first-main-run.md) — safe, bounded FIRST hardware run of src/main.py: deploy all src deps to /flash/lib, small arena + short timebox, walk each state glyph, abort = LEFT/RIGHT tap.
- [deploy-with-deps.md](./deploy-with-deps.md) — one command (`hub_programmer/deploy_deps.py ENTRY.py --apply`) that resolves a slot program's transitive local `src/` imports with `ast` and deploys each dependency to `/flash/lib` via the proven `upload.py`, then uploads+starts the entry via `slot_upload.py`. Resolver PROVEN on the host (main.py → 15 modules); the `--apply` orchestration is **[UNVERIFIED]** on hardware.

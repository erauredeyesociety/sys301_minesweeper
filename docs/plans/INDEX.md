# Plans — INDEX

Dated tactical "how" artifacts. The roadmap points here by path; detail never goes in the roadmap.
Naming: `YYYY-MM-DD-<slug>.md`. Delivered plans move to [../archives/plans/](../archives/plans/).

| File | Type | Summary |
|---|---|---|
| [minimalism-and-conformance-2026-09-09.md](./minimalism-and-conformance-2026-09-09.md) | ACTIVE-SPEC | **Aggressive-minimalism and bootstrap-conformance review, one day before Demo Day — host-only, hub never touched, nothing in `src/`/`examples/`/`scripts/` edited.** Built a real `ast` import graph over **99 files in all five code directories**: **6 of 23 `src/` modules are unreachable from `src/main.py`, 3 of them from nothing at all** (`telemetry`, `hub_distance`, `hub_selfcheck`), cross-checked against `deploy_deps.py`'s 16-module closure. **Cut NOW, all host-side:** `scripts/analyse-survey.py`'s closing verdict block — it is the **demo-day site-survey tool**, it still runs the REFUTED chromaticity rule, and it prints the **opposite of the measurement** ("MINES NOT RELIABLY DETECTED … YELLOW_NOTE", "needs the blue veto"), because its chromaticity filter silently discards **298 of 497** yellow samples — the **brightest** ones — before scoring; the shipped rule on the same file separates totally (tape/carpet **0.0 %**, mines **60–100 % ≥ 30**). Plus two rename orphans: `scripts/decode_telemetry.py:23` is **broken today** (`import config` binds a **ROS2 namespace package on this host**, proving the `config` shadow is a HOST problem too) and `probes/import_check.py:30`. Plus `main.py`'s dead `floor_anomaly` import (**15.1 KB / 2 fewer upload round-trips**). **Cut NOTHING from `src/` before Thursday** — deleting `telemetry.py` alone breaks **16 markdown links** and turns the repo's only standing guard red. **KEEP AS EVIDENCE:** `floor_anomaly.py` the file, `motion_tuning.py`'s 9 uncalled derivations, `hub_drive.py`'s geometry mirror (its hazard is **unproven-gone**, not gone). **Do NOT merge `main.py` onto `hub_drive`:** both turn paths derived to `motor.run(A,−v), motor.run(B,−v)` — **they agree**, so the merge buys nothing and costs two behaviour changes in a never-run program. Duplication table (geometry ×3, `MIN_TOTAL`/`MIN_CHROMA_TOTAL` ×2 from one commit, three lane-pitch models blocked on **KU-M33**), doctrine violations ranked by bite (ADR-0004 **edited in place** and its own grep now reports **17 false violations**; `CLAUDE.md`'s one-sensor "KNOWN BUG" is **false since this morning**; `main.py` is **not host-walkable** — `_now()` raises on the host while `hub_api.now_ms()` exists for exactly that; **7 `mission_config` knobs are read by nothing**), an explicit DEFERRED list, and 7 operator questions |
| [border-polygon-and-microslam-2026-09-08.md](./border-polygon-and-microslam-2026-09-08.md) | ACTIVE-SPEC | **The operator's "micro-SLAM": trace a hand-laid border, condense it LIVE to a minimal polygon, free the raw trace, and plan the lawnmower from what is left.** Leads with the verdict: **build none of it before Thursday** — `src/main.py` has never run, the shipped detector scores **yellow 0 % / blue tape 100 %** on this carpet, and `import config` still dies on the hub (KU-M38); this is the **Intro Report** deliverable. The design: a **gated integer corridor** (Reumann–Witkam strip + Opheim direction establishment) at **EPS = 25 mm**, one pre-allocated `array('h')` at **4 B/vertex, 272 B at a 64 cap**, expected **5–40 vertices**, overflow handled by **in-place EPS doubling** that returns `eps_final` as a number the planner inflates by. **Refutes the heading-threshold shortcut** — its sagitta error is proportional to radius, **102.7 mm at R = 12 m**, bigger than a lane pitch — and refuses an arc primitive (110 B saved, three new code paths). Closure gates at **5 % of driven path tested BEFORE Bowditch**, which our one real lap (8.5 %) **fails**; area is the shoelace accumulated online at 3 scalars, with the bowtie trap (**exactly 0.0**) and an **arena-free** isoperimetric gate. **Cuts the convex-hull rung** and closes the ladder's real hole: a partial-trace **AABB under-covers the arena** and must be gated on `|turn_sum| ≥ 300°`. Lane plan is min-altitude but the **heading comes from the FIRST traced edge** (min-altitude picked an edge worth **zero lanes** and **811 mm** of inherited cross-track), extents computed **on demand** for **12 floats / ~64 B constant in N**. Carries the re-derived **non-monotone pitch sawtooth** (P = 106 at S = 65, 58.5 at the as-built S > 76) and names the three repo sources that contradict each other on spacing. ⚠ Honest counterweight throughout: the reduction costs ~1 % of area error, **lap odometry costs 14–34 %**, nothing fits a 300 s slot, and `EPS = 25` is smaller than the only implemented trace primitive's own ±60 mm zigzag |
| [speed-and-heading-precision-2026-09-08.md](./speed-and-heading-precision-2026-09-08.md) | ACTIVE-SPEC | **The two problems the operator named — "make the motors turn faster" and "less than a sticky note of deflection over 10 ft" — answered from the logs already on disk.** ROOT CAUSE of the crawl: **not a bug and not a unit error.** `motor.run()` takes deg/s and the closed loop delivers it — commanded 120 dps achieved **119.1–119.5 on both wheels** across four legs `[MEASURED]` — while nine example files each hard-code their own expired bench-safety constant (80–250 dps) against a `config.TRAVERSE_SPEED_MMS` **no example reads**. CLOSES **KU-M36** with no hardware: `motor.velocity()` is **percent of rated speed, ×11.1**, fitted at **11.13** over 38 hand-turned pairs ≥250 dps against the 45603's 1110 dps rating — so `hub-api-surface:115` ("int deg/s") is wrong and speed must be read from `Δrelative_position/Δt`. THREE SPEEDS: **SEARCH 150 · FOLLOW 100 · TRANSIT 300 mm/s**, 2.3–5.5× anything ever driven. The ceiling is **not** the motor: the guaranteed worst-case chord is **36.48 mm** (a 76 mm square at 45°, and the cross-track error **cancels**), and the tick rate is not 20 Hz — **16.6 Hz measured** in the examples loop and **9.18 Hz in `src/main.py`** — so with the reflectance **blend penalty** (mid-yellow needs 43 % of a tick on the note) the max safe search speed is **157 mm/s at 16.6 Hz** and **87 mm/s in main.py, already below the shipped 150**. ⚠ RETRACTS "bias-vs-noise is answered": with n = 4 the 95 % CI on accumulated bias is **±7.3° over 3048 mm** against a 1.43° budget, the dataset is a **2026-09-03 pre-remount run sampled at 9.7 Hz**, and 3 of 4 legs share a repeatable **+10 to +17 ddeg start-of-leg transient** that a stationary settle cannot remove. **KU-M34 stays OPEN.** DERIVES the heading gain rather than tuning it — `3.3421 ddeg` per motor-degree of differential, `g = 6.684·T`, `z² − z + g·Kp`, unstable at `Kp = 2.49` — and lands on **`Kp ≈ 0.42`, which is what `follow_tape.py` already has**: the 40 s circle was the sign, not the gain. What changes is the **clamp (30 → 15 dps, framed as a runaway radius `R = 47.5·base/clamp`)**, the **guard** (the saturation counter **could not have fired** — `hold_ref` is re-taken 89 times in 670 rows), and a **once-per-run sign probe** with an 18:1 SNR. Carries three bench programs in the proven `motor_poc.py` slot pattern with a **byte-identical HEADER** so the decoders need no change, exact edits with line counts and a do-not-touch list, and a ranked risk table led by **`src/main.py` has never run (KU-M29)**, the **unmeasured colour-sensor update rate** (28.1 % of consecutive `rC` values are byte-identical at a 54 ms tick), **zero battery data under load**, and unexplained **253–813 ms stalls** inside the detection phase |
| [corner-arc-turns-and-recovery-2026-09-08.md](./corner-arc-turns-and-recovery-2026-09-08.md) | ACTIVE-SPEC | **The corner turn, rewritten as an arc, plus dead-end recovery** — the answer to the operator's "it detects the corner, turns the right way, and pivots onto nothing". One equation governs it: the sensor-pair midpoint ends the turn `e = X_v·sin θ − R·(1 − cos θ)` off the new leg's centreline, so the exact radius is **`R* = X_v·cot(θ/2)`** — at a square corner just the axle-to-vertex distance, which a pivot sets to zero. **CORRECTS THE BRIEFING TWICE:** `SENSOR_SPACING_MM` **cancels out of the radius** (it buys the tolerance `(S−W)/2`, not the value), and the outside sensor **provably cannot sweep across the perpendicular leg** on any arc that also lands the straddle — the operator's two sentences are instead the two *end-state* bounds of the capture window, with `R*` at its midpoint. Ships a fixed **`CORNER_ARC_RADIUS_MM = 65`**, which MEASURED-checks out for every `SENSOR_AHEAD_MM` in 38–92 mm at the tightest spacing, so **the ruler reading is not a Thursday blocker**; at 60 mm/s the arc turns at 52.9 °/s against the pivot's MEASURED 51.1, so `TURN_LEAD_DDEG` transfers. Three MEASURED findings the geometry did not need: the failing turn translates **2.2 mm across 87.2°** (a pure pivot, R ≈ 1.4 mm); a **false 90° turn already fired on channel totals of 3–4** (run 0000312454 seq 167–171) and `MIN_CHAN_SUM = 40` is in `find_corner.py` only; and **`TURN_LEAD_DDEG = 70` is wrong by 2.5×** — 7 logged turns give a 27.3 ddeg coast, which is exactly one loop tick. Inverts the corner rule to a **ceiling** (`W/sin 45° = 35.9 mm`), which lands in the empty gap of the 35 MEASURED on-tape episodes (all ≤ 22.2 or ≥ 46.5 mm). Carries the dead-end rule in millimetres, the 80-byte breadcrumb ring, pivot-then-drive-forward, an eight-row apply table with an explicit do-not-touch list, BM-C2 and its falsifiers |
| [junction-handling-and-boundary-trace-2026-09-08.md](./junction-handling-and-boundary-trace-2026-09-08.md) | ACTIVE-SPEC | **How the robot decides which way to turn at a junction, and how it traces only the outer boundary** — the two-days-out answer to the operator's "50 % chance of going the right way after the corner". **The sensors decide WHEN, the operator decides WHICH WAY:** `HAND` is captured from the arm tap (LEFT/RIGHT) and every accepted junction turns `900 ddeg * HAND`, which is deterministic at every convex corner and — for free — takes the boundary rather than the spur at the operator's overshoot T. Retracts *turn toward the sensor with the longer run*: MEASURED, port C held tape for **28 consecutive ticks over 54.6 mm with no corner present** (`…121202-followtape-0000153852.csv` seq 52-79), so the long run is the drift **ride-along** and the shipped `CORNER_SUSTAIN = 4` fires **114 mm early** in `…0000043283`. Replaces duration with **onset sharpness** (ground-truthed perpendicular crossings 5.3 / 8.3 mm vs a 17.5 mm ride-along; ⚠ separation is **1.4x, N=1 on the reject class**, BM-C1 closes it in 60 s), a **distance** lockout in place of the 1.5 s clock (= only 63 mm at the MEASURED 41.9 mm/s), and `find_corner.py`'s **`MIN_CHAN_SUM = 40`** floor — without it, blue fractions of 1.000 on channel totals of 1-4 can command a 90 deg turn. Names the genuinely **indistinguishable** pairs (corner vs outward spur, inset corner vs anti-hand spur, T vs 4-way, on-the-line vs lost), the ~216 B two-tier oriented map, the closure test led by the gyro turn sum, and an eight-row apply table against `examples/follow_tape.py` with an explicit **do-not-touch** list. ⚠ Ends by saying the quiet part: **`src/main.py` has never run**, the graded sweep is still one-sensor and still `DETECT_MODE="anomaly"`, so tracing is a prerequisite for nothing that is graded |
| [corner-turn-direction-2026-09-08.md](./corner-turn-direction-2026-09-08.md) | ACTIVE-SPEC | **Which way to turn at a corner, with two downward sensors and nothing else** — the answer to the operator's "50 % chance of going the right way after the corner", two days out. Key result, MEASURED across the two live `follow_tape` logs: **the two sensors never trip on the same tick at a junction** — one was continuously on tape for **4, 6, 7 and 23 ticks** before the other arrived, 0 ties in 4 junctions, minimum margin 8.9 mm of travel. **Turn toward the sensor that got there first**, arbitrated by continuous-on run lengths; the decision costs **0 s** because the data is already in the loop, and the turn still costs the MEASURED **1.71 s**. A **tie means a crossbar spanning both sides -> drive straight**, which is the T-intersection branch for one comparison (⚠ UNTESTED, 0 ties observed). Rejects the scan-rotate probe on geometry — a pivot sweeps the sensors on a **38 mm-radius annulus** and the robot stops **27 mm past** the corner — and nudge-and-test on the fact that *on the line* and *lost* look identical to a straddling pair (MEASURED 5.9 s between tape touches on a clean leg). Keeps the consistent hand rule only as the tie-break: it is 100 % on a convex box but **wrong at every inset corner** and turns onto spurs. Also lands two MEASURED bugs: `on_tape()` has **no low-light floor**, so 17–30 % of all tape trips are quantisation garbage (`find_corner.py`'s `MIN_CHAN_SUM = 40` was never ported), and the corner lockout is a 1.5 s clock = **67 mm**, against a 305 mm shortest leg. ⚠ **The correctness claim rests on ONE clean junction**, and the **fore-aft sensor mounting offset is [UNMEASURED] and can silently invert the decision** — BM-C1 closes it in 60 seconds with no new code |
| [border-trace-and-corners-2026-09-08.md](./border-trace-and-corners-2026-09-08.md) | ACTIVE-SPEC | **Boundary tracing, corner detection, and what a corner map is actually worth — two days from Demo Day.** Leads with the counterweight: blue tape measures `reflection()` **7–9**, *inside* the carpet band 3–9, so the brightness mine rule (threshold 30) **cannot see tape at all** and interior tape costs the count nothing — the map's value is lane termination, arena measurement and turn counting, never tape rejection. Defines a corner as a **sustained, sign-consistent change in the bearing between consecutive boundary TOUCH POINTS**, never as in-motion yaw (the one turn dataset we own disagrees with itself by 3.8–6.5° mid-rotation) and never as a line-follower loss (ruled out by the minimalism contract's ~25 mm mount wobble). **The loop does NOT close as flown:** the 1 ft square misclosed **108.3 mm on 1277 mm (8.5 %) with 30° of heading error**, and the 20× angular-closure fix is *fitted, not predicted* — its +7.45°/turn is probably a `TICK_MS=100` polling artefact. **Tracing can settle KU-P1 with a ≈27× margin, but one straight drive already does it.** Carries the ~80-byte corner map against a 12.7–208 KB raw path, the streaming extractor, the belt-and-braces lane termination (**tape always wins inside the window**), and the degradation back to today's odometry sweep as a VALUE change. Verdict: **corner demo before Thursday, perimeter lap after, for the report.** Runbook: [../runbooks/corner-demo.md](../runbooks/corner-demo.md) · program: [`examples/find_corner.py`](../../examples/find_corner.py) |
| [carpet-detection-and-blue-boundary-2026-09-08.md](./carpet-detection-and-blue-boundary-2026-09-08.md) | ACTIVE-SPEC | **The Demo Day detection decision, two days out.** Captures the two corrected mission facts (operator, 2026-09-08): the graded arena is a **CLOSED BOX of blue painters tape** — the practice area's incomplete tape is NOT the demo arena — and the floor is **multicolour carpet**. Key result, MEASURED on [../findings/runs/surface-survey-2026-09-08.txt](../findings/runs/surface-survey-2026-09-08.txt) by replaying the shipped modules: **the chromaticity anomaly detector fails silently on this carpet** — a yellow note scores 0.0 % above threshold on sensor C and 52.9 % on sensor D, while the **blue tape trips at 100 % on both**, so an unmodified sweep counts the boundary and misses the mines. The cause is darkness (`r+g+b ≈ 80` counts), not hue geometry, so `K_MAX` was never approached. **Decision: switch to plain REFLECTANCE** — carpet 3–9, blue tape 6–9, yellow 51–73 at the middle-hole mount, **zero overlap, a 42-point gap**, the shipped `calibration.calibrate()` accepting it at 57 contrast / 1.0 MAD, and the tape ignored for free so **no blue veto is needed**. ~28 lines across `config.py` and `main.py`, **zero new hub call sites** (`reflection == (100*i)//1024` exactly, 0 mismatches in 3412 rows), `detector.py`/`sweep.py`/`result.py` untouched. Carries the go/no-go survey runbook with numeric GO/MARGINAL/STOP criteria, a five-rung fallback ladder, and the standing warning that **first-run risk (KU-M29/KU-M30) and the open units question (KU-P1) dominate all of it** |
| [competitive-interference.md](./competitive-interference.md) | ACTIVE-SPEC | **Interfering with other teams' robots is authorized in this competition** (operator, 2026-09-01) — captured as scope. NOT built yet on operator direction. What matters NOW: our own hub is on the same BLE control plane, so defense (short advertising window, prefer USB, fail-safe on link loss) is a `main.py` requirement regardless. Offense deferred until the professor confirms the rules in writing. |
| [next-session.md](./next-session.md) | ACTIVE-SPEC | **START HERE next session.** The ordered plan, grouped by **what each item NEEDS** — professor only · teammate only · hub over USB · hub with one variable changed · a colour sensor (no robot) · the robot built · a keyboard. Class time is the scarce resource, so whatever is present at the start can be worked immediately. Leads with the 60-second state, names the three real blockers, carries the residual unknowns forward, and ends with **what NOT to redo** — the API generation, the deploy route and Bluetooth are all closed by measurement |
| [first-hardware-session.md](./first-hardware-session.md) | ACTIVE-SPEC | **What happens the moment the hub is plugged in, in dependency order.** Host prep → is it seen → Hub OS identification (the gate) → Bluetooth → walking skeleton → telemetry → measurements. Plus what does *not* need the hub and should not wait |
| [known-unknowns.md](./known-unknowns.md) | ACTIVE-SPEC | Everything we do not yet know, grouped by **how it gets resolved** — ask the professor · measure · ask a teammate · decide ourselves. Check it before building on any assumed value. **CONSOLIDATED 2026-09-08 at the operator's request: ~50 live rows → 27**, restructured into **LIVE** (something before Demo Day depends on it) · **PARKED** (still unanswered, nothing depends on it — one line each) · a **CLOSED ledger** (every row ever closed, with its answer, date and evidence). The day closed or retired 20 rows — **GATE 1 (KU-M22/M32/M6)**, the floor, the tape, the detection scalar, the sensor's usable range, the ~3 mm coast, carpet slip, the 20 Hz loop rate, FR-2b demonstrated — and opened 8: **KU-M33** sensor spacing (highest-priority measurement) · **KU-M34** heading wander, bias or noise · **KU-M35** lap-scale drift · **KU-M36** `motor.velocity()` unit (11.7–12.6× off) · **KU-M37** Hub OS relaunch without a power cycle · **KU-M38** `config` is shadowed on the hub · **KU-D10** corner-turn radius law · **KU-D11** the shipped code is a one-sensor robot |
| [risk-register.md](./risk-register.md) | ACTIVE-SPEC | 19 project risks, scored likelihood × impact and ranked, each with a mitigation, a contingency, an owner role, and a trigger. **Updated 2026-09-08: five risks CLOSED** (R-04 deploy route, R-06 SPIKE generation, R-07 ModemManager, R-09 the CSER template, R-12 colour separability — all kept in place with their reason, because the report needs the ones that did *not* happen). **R-01 has REALIZED** and is re-scored L 3 → 5, exposure 25: at 10 ft a one-sensor sweep is ~69 min [COMPUTED] and no question can reduce it further. **Two new:** **R-18** — `src/main.py` has never run and its first run would be on Demo Day (L 4 × I 5 = 20) · **R-19** — the mission code sweeps a ONE-sensor swath while two sensors are mounted (L 4 × I 4 = 16) |
| [mission-algorithm.md](./mission-algorithm.md) | ACTIVE-SPEC | **The program the robot runs, start to finish** — the one document a programmer can write `src/main.py` from without re-reading the research. Eleven-state run machine (including **ABORT** and **CALIBRATION_FAILED**), the run-start calibration, the per-tick order with **one colour-sensor call per tick** and a `None` guard before every unpack, how the six pure modules compose, the 5×5 matrix report pages, one parameter table keyed to Q1–Q5 and BM-0–BM-9, and **fifteen decided degraded modes**. **Names every function that does not yet exist, with its signature**; closes gap G-3; makes the unmeasured loop rate self-measuring at run start. Audited 2026-08-26 — see its revision history for what the audit changed |
| [competition-program-design.md](./competition-program-design.md) | ACTIVE-SPEC | **The competition program design of record for the robot AS BUILT** — differential drive + fixed rear roller-ball + **two** front-corner colour sensors. Refines (never replaces) [mission-algorithm.md](./mission-algorithm.md): gyro-closed spin turns and gyro heading-hold commanded in **percent/degrees** (mm not convertible until BM-3/BM-4); the second sensor is **one number** — `pass_pitch = lane_pitch + SENSOR_SPACING`, two `EdgeCounter` streams de-duped by a new `MineLedger`, a left/right skew cue, mine-vs-boundary via the existing `classify`; run shape is the existing state machine with **output-only** BLE telemetry and a link-drop fail-safe that leaves the drivetrain untouched. Carries the adversarial-challenge corrections (the ±180 seam is handled by `normalize_angle` not avoided; `boundary` must NOT enter `config.CLASSES`; don't pin sensor spacing to the validity edge; SPIKE-3 beep UNVERIFIED). **Top blocker unchanged: the units of "10×10."** New signatures named; every other file gets a recommended-changes row, not an edit |
| [minimalism-contract-2026-09-03.md](./minimalism-contract-2026-09-03.md) | ACTIVE-SPEC | **The contract that keeps `src/main.py` a switchboard, not a framework** (operator steer 2026-09-03: keep it open, stick to basics, some parameters we skip on the day). Binds every open unknown to ONE config knob and classifies it **REQUIRED / OPTIONAL / SKIPPABLE**, with the graceful degradation for each skip. Defines the **irreducible core** — `ARMED → CALIBRATE_FLOOR → READY →(countdown)→ SWEEP (odometry + timebox, report-as-you-go) → REPORT` on **anomaly presence-only**, needing no target sample, no colour, no boundary sensor, no classifier — with every OPTIONAL feature a bolt-on toggled by a knob. Carries the **≤8-knob competition-day tuning card** (with the structural gates marked DO-NOT-TOUCH) and the explicit **what-NOT-to-write** list: no persistent mine map (mines move), no default classifier, no line-following (mount wobble), no live SLAM. An answer changes a **value**, never a state. |
| [competition-operations-2026-09-03.md](./competition-operations-2026-09-03.md) | ACTIVE-SPEC | **The operations design on the proven slot mechanism** — deploy/program-swap/start/stop/reset/retrieve, refining (not duplicating) [mission-algorithm.md](./mission-algorithm.md)'s run machine with **one added `ARMED` wait-state** that fixes the real bug (`slot_upload` always Starts, so without a wait the robot would calibrate on the Programmer's desk), a countdown *loop* on the `READY→SWEEP` edge, and a reset re-entry. STOP is a side-button **press** (AB1), never a LEFT-hold — the centre button is the firmware's. Plus the **significant-event telemetry filter** (a pure `significant_event(prev,cur,event)` predicate, edge-triggered off the detector's `Event`, events-only by default — not a three-tier ring; only `EVT_DETECTION/EVT_REJECT/EVT_STATE` are implementable today) whose `(pose, observation)` rows feed a minimal dead-reckoned mine map + discovered arena rectangle; and **retargetable colour** (config `TARGET_COLOUR_NAME` keyed so `CLASSES` == the label so the report agrees with itself; signature learned in-field; specular → presence-only + UNKNOWN). **Single go/no-go: does a slot program survive USB unplug?** |
| [competition-program-readiness-2026-09-09.md](./competition-program-readiness-2026-09-09.md) | ACTIVE-SPEC | **Adversarial review of the 2026-09-09 brightness port of `src/main.py`, one day before Demo Day — host-only, hub never touched.** Verdict: **NOT fit to run**, stopped by ONE line — `src/hub_color.py` uses a bare `_color` that is never imported (lines 29/46/74), REPRODUCED under stub SPIKE 3 modules as a `NameError` that kills `calibrate_floor()` at its first sample while `read_reflection_second()`'s blanket `except Exception` swallows the same fault silently. The `import config` shadow (KU-M38) is **already fixed** — the `mission_config` rename has landed across all 9 modules. The brightness port ITSELF is **correct**: burst and run sample the identical `fuse(read_reflection_pair())`, `signal()` is the identity at polarity +1, the lane-end `finish()` fix and the 64-entry ring are right. The defects are in the machinery AROUND it, and were run on the host, not argued: `do_sweep()` returns **COMPLETE with lanes=0/75 and `is_trustworthy()==True` in 0 virtual ms** when both sensors are dead (COMPLETE is decided by the plan's internal index, not `lanes_completed`); the timebox clock starts at PROGRAM start so up to **433 s** of operator waiting is charged against a 300 s box; calibration **creeps 450 mm = 14.8% of the arena** un-subtracted and detector-off; the arming gate is a bare `max()` that REFUSES on one fleck of 16 **or on a real mine lying in that creep path**; there is **no top-level `except`**, so all ~30 unverified hub call sites fail as a frozen glyph with no tone; and the report shows a bare `13` with **no denominator** while `describe()`/`is_trustworthy()`/`coverage_fraction()` are called from **nowhere** — a simulated healthy 300 s run ends `lanes=13/75(17%)` reported as one number. ⚠ **Four FALSE ALARMS separated out so nobody re-fixes them**: passing `WORST_CASE_CHORD_MM` to `event_width_gates()` is **WRONG and loses mines** (ran it — gates (2,6) reject the 6.74-sample centred and 9.53-sample diagonal crossings of a real note; the shipped (2,13) accepts both and still rejects a 14-sample merged pair); the **zero-radius pivot is CORRECT** (`sweep.py` emits TURN 90 → DRIVE pitch → TURN 90, not a 180 — the arc's miss term is a LINE RE-ACQUISITION error with no analogue here, and an arc would ADD an uncommanded `R(1−cos θ)` and make the pitch WRONG); **motor safety is sound** (LEFT starts and stops before RIGHT, so a single-port fault cannot leave a wheel spinning); and the three lane-pitch models are **one model already settled at 41 mm** — do NOT raise it, `SENSOR_SPACING_MM` is [UNMEASURED] (KU-M33). Buys the turn-error fix with a VALUE not new control code: `TRAVERSE_SPEED_MMS` 150 → 100 drops the spin from 180.9 to 120.6 deg/s AND clears the p95 sampling ceiling of 104.8 mm/s. Carries a 7-run **staged bring-up** (pyflakes → probe every uploaded module name for the shadow → held-off-floor state-machine walk → arena arming → one note → three notes), the **minimum viable demo** (`SweepPlan` ALREADY takes the region: two constants declare a **762 mm square swept 100% in ~119 s** instead of 17% of the arena, with `examples/find_note.py` as the zero-code fallback), a mermaid report-state diagram, and **8 contradictions with `../runbooks/demo-day.md`** — above all its §4 "thresholds calibrated at run start, not hard-coded", which the fixed-threshold brightness rule has superseded. |
| [conops.md](./conops.md) | ACTIVE-SPEC | **Concept of Operations** — how the robot is actually used: actors, operating constraints, the eight-phase nominal run, and six operational scenarios including a mine on the boundary, drift, and an ambiguous colour reading |
| [requirements-traceability.md](./requirements-traceability.md) | ACTIVE-SPEC | **Traceability matrix** — every FR/TR/RR traced to its source, its design element, and its verification method. **Names the holes:** FR-2b is UNDERIVED and FR-6 has no design element |
| [verification-plan.md](./verification-plan.md) | ACTIVE-SPEC | **How each requirement gets proven** — method, procedure, pass criterion as an observable, evidence location. Splits host-verifiable-today from blocked-on-hardware. **GATE 1 is the colour separability go/no-go** |
| [bench-measurement-plan.md](./bench-measurement-plan.md) | ACTIVE-SPEC | **The one class period that replaces the `[ASSUMED]` numbers with measurements** — ten measurements ordered by what unblocks what, each time-boxed and each named to the config constant or document it changes. Colour separability is the gate; **BM-3 effective rolling diameter is the keystone**; the drop order is explicit because the list is 101 minutes against 35. Executed by [../runbooks/measure-drivetrain.md](../runbooks/measure-drivetrain.md) |
| [sensor-suite-architecture.md](./sensor-suite-architecture.md) | ACTIVE-SPEC | **What mix of sensor TYPES to buy, and which movement patterns each mix unlocks** — the companion to the coverage trade study, which answers *how many colour sensors*. Key result: **Q3 (boundary type) picks the sensor types; Q1×Q2 pick the colour count**, and they are separable buys. Enumerates the 10 realistic port configurations against 4 free ports, ranks seven purchases by thoroughness per Schrute Buck, and answers the operator directly: **a second distance sensor buys heading at 8–16° against a 0.28–1.13° budget — a force bumper buys 0.3–0.6° for the same money.** Buy one colour sensor now; the decision tree keys the rest on Q1/Q2/Q3 |
| [sensor-decision-matrix.md](./sensor-decision-matrix.md) | ACTIVE-SPEC | **One table joining sensor count, orientation, ports and cost.** Answers "1 colour + 2 distance?" — no, that branch is never taken. Buy one colour sensor now; everything else waits on Q3 |
| [questions-for-the-professor.md](./questions-for-the-professor.md) | ACTIVE-SPEC | What the verbal briefing left open, ordered by how much the answer changes the build. **Ask Q1 (units) and Q2 (time limit / scoring) first.** Three partial answers landed 2026-08-27; Q3 closed, Q5 halved, and **Q0 was retracted rather than closed** because the answer contradicts itself |
| [blind-teleoperation.md](./blind-teleoperation.md) | FORWARD-PLAN | **What a permitted-but-blind human operator would actually buy, and why we stay autonomous.** Key result: **a blind operator refunds NO navigation work** — this retracts the standing claim that a human driver removes sweep planning, odometry and the coverage-time problem, because its justification was *"a human covers the area by eye"* and the operator may not look at the arena. Computed on the 10 ft branch: **371 discrete commands, +12–49% run time, zero information gain**. Records what the hub actually has (a full MicroPython `bluetooth` module — overturning our earlier inference) against what has actually been exercised (**nothing**, and our hub was not advertising in the one scan we ran). Recommends autonomy as the baseline with read-only supervision as a **gated stretch**, and names what NOT to build yet. **No ADR while the premise quote is self-contradictory** |
| [purchasing-strategy.md](./purchasing-strategy.md) | ACTIVE-SPEC | **The Schrute Buck economy as a resource-management problem** — the buy/defer rule, the buy order sequenced by what it unblocks, and a 14 SB reserve derived from named liabilities. Key result: reversing a purchase costs `ceil(P/10)` SB — 1 SB at either price this project has actually paid, against 4 SB for one billable minute of a four-person meeting — so we decide in writing and buy cheap parts on sight. **§1 is the card the Supplier carries to the store** |
| [2026-08-25-sprint-1-walking-skeleton.md](./2026-08-25-sprint-1-walking-skeleton.md) | ACTIVE-SPEC | Prove the Ubuntu → hub → run → read-back loop end to end before any mission code |
| [analysis-detection-quality.md](./analysis-detection-quality.md) | FORWARD-PLAN | **What to compute from a logged reflectance stream to know whether the detector is any good** — the companion to [analysis-motion-quality.md](./analysis-motion-quality.md), and the payoff for logging raw samples instead of decisions. Key result: the threshold sweep is **two** parameters (centre × hysteresis), swept in contrast-fraction units so the answer transfers between floors, and **its output is a plateau list, not a curve** — a wide plateau is a robust setting, two plateaus of different counts means the data does not determine the count. Four metrics need no ground truth (event widths, normalised peaks, separation achieved vs predicted, and a rejection ledger got free from one gateless replay); **margin(t) in units of floor noise is how a log reveals a calibration that expired mid-run**. Argues a confusion matrix is **overkill for presence** (the true-negative cell is undefined) and correct for classification. Ends with six ranked items for `./data_analysis/` and seven named rejections |
| [telemetry-and-analysis.md](./telemetry-and-analysis.md) | FORWARD-PLAN | Getting run data off the hub and analysing it on the laptop — the substitute for a test suite |
| [analysis-motion-quality.md](./analysis-motion-quality.md) | FORWARD-PLAN | **What to compute from logged encoders + gyro to know whether the robot drove where it thought it did.** Recovers `WHEEL_DIAMETER_MM` and `TRACK_WIDTH_MM` from two drives instead of a ruler (KU-M3), and calibrates the gyro's own scale factor as a by-product. Key result: **gyro-vs-encoder divergence is the highest-value diagnostic and costs nothing extra** — the *shape* of the divergence separates diameter mismatch from a wrong track width from a slip event. Says plainly that a cross-track number from odometry alone is **circular**, and that **reduced UMBmark (3+3, ~20 min) is confirmation, not calibration, and is the first thing to cut**. Ends with a 5-function, ~155-line implementation contract for `./data_analysis/`. **Audited 2026-08-26** — carries a standing correction to [../runbooks/measure-drivetrain.md](../runbooks/measure-drivetrain.md) §6.5, whose Type A / Type B pairing is inverted relative to Borenstein |
| [telemetry-over-bluetooth.md](./telemetry-over-bluetooth.md) | FORWARD-PLAN | **Untethered telemetry over BLE, and the analysis tools that consume it** — extends the row above with the Bluetooth-specific design. From LEGO's own published protocol reference: the hub streams IMU, both encoders, raw colour RGB and distance on request (`DeviceNotification`), and `print()` has a wire representation (`ConsoleNotification`) — but **there is no file-download message, so nothing can be pulled off the hub as a file**. Byte-level framing arithmetic — **run through LEGO's own `cobs.py`, not estimated** — says **design for 20 Hz, not the 100 Hz in config.py**; the link rate itself is a bracket that only a bench measurement (gate G3) can close, because **no published field reports the hub→host rate**. Recommends the hybrid: live CSV + slow binary witness + a fixed on-hub ring buffer. Also: why the cable matters — **0.71° of heading bias eats the entire cross-track budget** — the record format with its run-context header, specs for `analyse_run.py` / `plot_run.py` / `capture_run.py`, and a 15-minute bench go/no-go whose **G4 is the crux gate** |
| [2026-08-25-docs-rag-and-literature-workflow.md](./2026-08-25-docs-rag-and-literature-workflow.md) | FORWARD-PLAN | Whether to stand up a docs-rag, and how to handle academic PDFs in the meantime |
| [2026-08-25-coverage-strategy-trade-study.md](./2026-08-25-coverage-strategy-trade-study.md) | FORWARD-PLAN | Seven coverage options scored against weighted criteria; the ranking flips with the scoring rule, so the answer is a **decision table** of Q1 (units) × Q2 (scoring). Buy one colour sensor now; the 2nd and 3rd wait for the answers |
| [movement-control-laws-2026-09-03.md](./movement-control-laws-2026-09-03.md) | SIDE-CAR | **The actual control laws behind two of [competition-movement-options](./competition-movement-options-2026-09-03.md)'s modes**, concrete enough to implement. **Law 1 — two-sensor line straddle:** error `E = dev_L − dev_R` (two-sided, polarity-free) → PD `turn_pct`, output matching `heading_hold_pair`; and the geometry gate that decides if it works at all — **workable only for `S − phi < w < S + phi`** (line thinner than the gap = dead-band/invisible; wider = both saturate), so at `S≈57`/`phi≈12` a 2-inch tape works and a 1-inch tape dead-bands. **Law 2 — corner-start lawnmower with online mapping:** square-up via two-sensor edge order (`skew = atan(dS/S)`), reset yaw, pose `(0,0,0)`, boustrophedon with gyro-closed caster-aware 90° turns and `pass_pitch = lane_pitch + S`, re-square each lane end, and **discover the arena rectangle from four detected edges** because the units are unknown. Both are additive pure signatures (`detector.straddle_error`, `odometry.line_straddle_pair`); nothing is applied. **Bench tests that confirm each:** the lateral-offset transfer curve (Law 1) and the known-angle skew-recovery test (Law 2) |
| [2026-09-08-operator-briefing-corner-turns-to-competition.md](./2026-09-08-operator-briefing-corner-turns-to-competition.md) | 2026-09-08 | **The operator's design briefing that sets the remaining work**, archived verbatim plus a structured extraction. The corner turn fails because it is a ZERO-RADIUS PIVOT — it rotates without translating, so the perpendicular tape ends up outside the sensor pair. The turn needs a radius large enough for the OUTSIDE sensor (relative to the turn) to sweep across the tape's full width. Also: a corner is a turn over ~45 deg; the border may be an arc or a squiggle, not straight; a dead end means backtrack and pivot 180; and the competition program is trace -> condense to polygon vertices -> free the raw trace -> lawnmower ("micro-SLAM"). Names SENSOR_SPACING_MM as the blocking [UNMEASURED] value |

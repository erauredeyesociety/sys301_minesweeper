# Findings — INDEX (INTERNAL)

Discoveries about **our own** robot, code, and setup: measurements, root causes, audits. Anchored to a
real observation with its conditions — never a conclusion without the measurement behind it.

| File | Summary |
|---|---|
| [host-environment.md](./host-environment.md) | Ubuntu host readiness for SPIKE Prime USB serial — ModemManager is active and will corrupt the first hub session |
| [coverage-time-budget.md](./coverage-time-budget.md) | ⚠ If "10×10" means feet, exhaustive single-sensor coverage is 125–204 m of driving (8–23 min) — a design problem, not a tuning problem |

**Expected next entries:** the Hub OS / API generation identification result, and the reflected-light and
per-color sensor values measured on the actual arena floor.

**Record the measurement, not just the conclusion** — units, surface, lighting, date. This folder is
where the Intro Report's results section comes from.

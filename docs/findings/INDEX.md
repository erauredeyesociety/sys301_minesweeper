# Findings — INDEX (INTERNAL)

Discoveries about **our own** robot, code, and setup: measurements, root causes, audits. Anchored to a
real observation with its conditions — never a conclusion without the measurement behind it.

| File | Summary |
|---|---|
| [main-py-irreducible-core-2026-09-03.md](./main-py-irreducible-core-2026-09-03.md) | 2026-09-03 | **src/main.py written** as the irreducible-core switchboard: ARMED -> CALIBRATE_FLOOR -> READY -> countdown -> SWEEP -> REPORT, DETECT_MODE=anomaly default, optional features are knob bolt-ons. Imports on host + check-docs green (STRUCTURE verified); **every hub call site is [UNVERIFIED]** -- never run on the robot. Added src/hub_runtime.py (runloop wrapper) + config DETECT_MODE/COUNTDOWN_S. |
| [square-drive-fusion-2026-09-03.md](./square-drive-fusion-2026-09-03.md) | 2026-09-03 | **MEASURED 1 ft square drive (motors vs IMU).** Robot drives STRAIGHT (drift <1 deg/ft -- the spins-left issue is resolved); wheel dia 63.5 mm confirmed; **track width MEASURED 95 mm** (was assumed 176); turns under-read ~5 deg = a logging artifact, now fixed by a segment-boundary log row. decode_telemetry.py gained an automatic per-segment phase breakdown. |
| [standalone-run-and-retrieve-2026-09-03.md](./standalone-run-and-retrieve-2026-09-03.md) | 2026-09-03 | **MILESTONE: the robot runs a program AUTONOMOUSLY while unplugged, and its telemetry is retrieved after replugging.** A slot program (uploaded as program.py) logged IMU+colour+battery to /flash across a 45 s run with the USB cable OUT for ~20 s; replugged and pulled back 412 rows spanning the unplugged window. Proves both the untethered-run mechanism and the record→retrieve fallback. No Bluetooth needed. |
| [sensor-fusion-and-slot-wall-2026-09-03.md](./sensor-fusion-and-slot-wall-2026-09-03.md) | 2026-09-03 | **Sensor fusion works over USB** — motors+IMU+colour captured together, decoded, and the encoder-vs-gyro fusion caught a real slip/held fault (wheels spun 4.5 turns, robot didn't rotate). Also documents the **slot-execution wall**: slot_upload ACKs 'program started' but the program never runs (motor works via REPL, not via slot); runloop didn't fix it. BLE telemetry MTU is 517, not 23. |
| [telemetry-pipeline-validated-2026-09-03.md](./telemetry-pipeline-validated-2026-09-03.md) | 2026-09-03 | **The on-hub log → USB retrieve pipeline works end to end** — 30-column, 300-row CSV logged to `/flash/tmp/telemetry`, pulled over USB SHA-verified. Needs NO Bluetooth. Proves logging+retrieval; real-motion data and the untethered slot path are the next runs. |
| [classroom-transcript-2026-09-01.md](./classroom-transcript-2026-09-01.md) | 2026-09-01 | **Second relayed classroom conversation**, with strict attribution (quoted-professor / teammate-idea / ambiguous). New content: the class economy (contract roles from other teams, no-frame build, 90% sell-back, parts scarcity), competition rules (roughly A/F grading, mines may sit near the boundary), and teammate design ideas (lawnmower+pivot, start-in-corner dead-reckoning, colour+position fusion, width discriminator, port-swap variants). "12×12" was a hypothetical — does NOT answer the units question. Adds professor questions. |
| [drive-checkpoint-2026-09-01.md](./drive-checkpoint-2026-09-01.md) | 2026-09-01 | **CHECKPOINT: the drive stack works.** Forward/back/turn-right/turn-left all commanded, moved, and confirmed by eye. Locks port A=left / B=right and the mirror sign flip (forward = `A:-v, B:+v`) — MEASURED, no longer assumed. Direct drive → 1 rev = 360 encoder-deg. Speed control is the velocity arg (±930 dps). Encoders symmetric to ±1°; ~9° ramp loss on a short move. Pending: wheel diameter for distance. |
| [colour-first-look-2026-09-01.md](./colour-first-look-2026-09-01.md) | 2026-09-01 | **First real colour readings, on substitute surfaces (name cards, paper, khaki), held <1 cm from sensor C.** Closes KU-M20: `rgbi()` is **0–1024**. Red separates trivially (55% red fraction). ⚠ **Shiny blue and green cards saturated both channels at 1024 and became indistinguishable** — the specular / blue-on-blue risk, measured. `reflection()` marks presence not colour. Usable standoff is narrow: <1 cm saturates gloss, 51 mm reads carpet as nothing. Needs matte yellow + real tape at ~16 mm for the true GATE 1. |
| [hub-api-surface-2026-09-01.md](./hub-api-surface-2026-09-01.md) | 2026-09-01 | **The consolidated MEASURED API reference for the robot AS BUILT** — one section per device actually present: 2 motors (id 48, `max_speed` **930 deg/s** not the assumed 660), **TWO** colour sensors (id 61, ports C/D — not one), IMU, light matrix, sound, buttons. Every method labelled MEASURED (a value read this window) vs API-only **[UNVERIFIED on our hub]** (in `dir()` but the call not run) — no motor was moved. Full constant tables read off the firmware: `motor.status` (**DISCONNECTED=5** named, closing KU-M15; ⚠ `CONTINUE`=`CANCELLED`=3), stop actions, `color` (YELLOW=7, no GREY/SILVER), faces, gestures, orientation, buttons. `rgbi()` **exceeds 255** (intensity 568), true ceiling still [UNVERIFIED]. `distance_sensor`/`force_sensor` modules present but **NOT owned**. Flags the light-matrix `IMAGE_*` glyphs and `hub.sound` as the feedback channel for a screenless robot |
| [ble-protocol-2026-08-27.md](./ble-protocol-2026-08-27.md) | 2026-08-27 | **Bluetooth works, and our hub is provably ours.** Connected from Linux with raw `bleak`, no LEGO software. LEGO's COBS framing implemented and **validated against real responses in both directions** — `InfoRequest` frames to `00 00 02`, and `DeviceUuidResponse` returned `0397000036001b00145030514b323320`, **identical to the UUID read over the USB cable**. That is the classroom identification problem solved: connect, ask, compare, disconnect if it is not yours — never trust the name or the MAC. Transport is service FD02 with one write characteristic and one notify. **Negotiated MTU 23 against an available `max_packet_size` of 509**, a 20× throughput gap that must be closed before any telemetry design. Hub OS/RPC version 1.8.149 is numbered differently from `os.uname()`'s MicroPython build. CONNECT light: blinking = advertising, solid = connected. |
| [mission-answers-2026-08-27.md](./mission-answers-2026-08-27.md) | 2026-08-27 | **Provenance record for the mission answers relayed by a teammate** — what was QUOTED, what was RELAYED, what is our own INFERENCE, so the Intro Report attributes correctly. Mines are yellow (*"we expect yellow"* — **hedged in the source**, and the second hedge after *"I think"*). **No walls: the boundary is floor tape**, blue painters **or** silver/grey duct, unchosen. ⚠ **The autonomy answer contradicts itself** — *"you can't have a human operator... if you do have a human operator, they cannot be looking at the arena"* — so it is **not** recorded as answered. The **units of "10×10" are untouched and still first**. Ends with the seven follow-up questions the answers created |
| [hub-first-contact-2026-08-27.md](./hub-first-contact-2026-08-27.md) | 2026-08-27 | **First contact with the real hub, over USB.** Closes the project's biggest unknown: this is **SPIKE 3 / current API** — `motor`, `motor_pair`, `runloop`, `color_sensor` present, **no `spike` module**, so every SPIKE 2 tutorial is inapplicable. Our hub's permanent identity (`device_uuid 03970000-3600-1B00-1450-30514B323320`) read over the cable so a BLE advertisement can be matched to it in a room full of hubs. `/flash` is a stock MicroPython filesystem with an empty `main.py` and 32.4 MB free — **the LEGO app is not required to reach the hub**. `color_sensor.rgbi()` exists, so raw RGB thresholding is available and the built-in colour ID can be bypassed; **there is no GREY or SILVER colour constant**, which matters if the arena boundary is silver duct tape. The `bluetooth` module is the full MicroPython BLE stack, **overturning our earlier inference** that a hub program cannot drive its own radio. Nothing was written to the hub. |
| [firmware-integrity-proof.md](./firmware-integrity-proof.md) | 2026-08-27 | **PROOF that the hub's firmware was never modified**, not the assertion. The crux, stated early: the *firmware* is the MicroPython binary in the STM32F413's internal program flash; `/flash` is the FAT filesystem that firmware **exposes**, and writing a `.py` there is saving a document — it goes *through* the thing it would have to modify. A baseline was captured before anything was written, re-captured after, and diffed: the **complete** difference is four lines — a new `lib` directory, **8 blocks (32 KB, ~0.1% of free space)** consumed, and battery 7942→8001 mV / temperature 247→251 because the hub was charging over USB. **Every stock file byte-identical; identity, module list and API surface unchanged.** Every line of the diff is walked through, including the 3 of 8 blocks that arithmetic does **not** account for. Lists by name what was never done (no DFU, no bootloader, no `machine.reset`, no `vfs.mkfs`, no format, no factory reset, no update prompt, no LEGO app), gives the re-runnable capture+diff commands, and tells a future reader exactly what to do if the diff is ever dirty — a hunk in `01`/`02`/`03` is the firmware moving: stop and get the operator |
| [imu-characterisation-2026-08-27.md](./imu-characterisation-2026-08-27.md) | 2026-08-27 | **MEASURED on our hub — the IMU's units, wrap, read cost and drift.** Units *derived from gravity, not from a datasheet*: a still hub reads `|a| = 989.3`, so `acceleration()` is in **milli-g** (~989 per g); true tilt 0.705° against a reported 6.7 gives ratio **9.53**, so `tilt_angles()` is in **decidegrees** — `hub_imu`'s `/10.0` and `telemetry`'s `*_ddeg` columns are confirmed right, and `accx/y/z` should become `*_mg`. **Yaw wraps at ±180°** (observed −1795 / +1771 ddeg), so every heading delta must go through `odometry.normalize_angle()`. A full IMU tick costs **1.350 ms** = 14 % of a 10 ms budget — but ⚠ **an unresolved anomaly**: the three calls sum to 0.328 ms individually, **4× less** than measured together, so a repeated read may be returning a *cached* value. Plan with 1.350 ms; never quote the per-call figures as read rates. **Drift: 0.0033 °/s over one clean 30 s stationary window** — and a 30 s run showing 3.29 °/s was **DISCARDED** because the drift reversed sign while the operator handled the robot. Drift while *driving*, and over a run-length window, is still **UNMEASURED**. Closes no `config.py` value |
| [host-environment.md](./host-environment.md) | Ubuntu host readiness for SPIKE Prime USB serial. **Both blockers CLEARED 2026-08-27** by `scripts/setup-host.sh --apply`: ModemManager is now `inactive`/disabled and `/dev/spike` is a stable symlink. ⚠ And the honest correction: `mmcli -L` returned `No modems were found` with the hub plugged in, so ModemManager had **not** in fact grabbed this device — the mitigation is a kept precaution, not a fixed fault |
| [cser-template-libreoffice-roundtrip.md](./cser-template-libreoffice-roundtrip.md) | **MEASURED** — the Intro Report template survives LibreOffice: all 20 `Els-*` styles and the trim size intact; styleId hyphens stripped (harmless); one sample image and one OLE object lost (they get replaced anyway). Assemble in LibreOffice; open once in real Word before submitting |
| [coverage-time-budget.md](./coverage-time-budget.md) | **Rewritten 2026-09-01 for TWO colour sensors** (MEASURED, ports C and D) and a **MEASURED 930 deg/s** motor ceiling. Two sensors on one bar multiply the pass pitch by **2.59×, not 2×**, because the gap *between* them is charged **build tolerance** instead of odometry error — so the second line is cheaper than the first, and **the size of the win is the Designer's unmade choice of spacing** (41 mm → 2.00×, 65 mm → 2.59×, above ~71 mm coverage stops being guaranteed). At 10 feet that is 75 passes / 229 m cut to **29 passes / 88 m**. ⚠ **But run time is still not a number: deg/s does not become mm/s without the wheel diameter, which is UNMEASURED**, so the answer is a formula plus a table over Ø24/Ø56/Ø88 — and at 10 ft with two sensors that table spans **4.0–10.9 min**, a 2.7× spread that **one ruler deletes**. What flips under a 5-minute gate: 10 feet presence-only on Ø88 (10.4 → **4.0 min**), 10 × 30 cm tiles on Ø88, and every 1.52 m cell under classification. What does not: **no 3 m arena reaches 5 minutes with classification at any wheel or sensor count**, and a 3-minute gate at 10 ft stays refuted. Second, quieter win: two sensors cut the sensitivity to the unmeasured cross-track error from +95 % to +24 %. Also settles the 41 vs 46 mm lane-pitch convention in favour of `config.py` |

**Expected next entries:** the cache-vs-real-read probe that settles the IMU timing anomaly (**KU-M14**), gyro drift measured *while driving* (**KU-M9**), whether `/flash/main.py` autoruns at boot (**KU-M16**), how long the BLE advertising window stays open (**KU-M17**), the numeric `motor` status constants (**KU-M15**), and the reflected-light and per-colour sensor values measured on the actual arena
floor, and the three-way separability of **floor vs boundary tape vs yellow note** — blocked on owning a
colour sensor and on knowing which tape (KU-P13).

**Record the measurement, not just the conclusion** — units, surface, lighting, date. This folder is
where the Intro Report's results section comes from.
